"""Auth subsystem — email/password + JWT in HTTP-only cookies.

OAuth, SSO, 2FA, password reset, email verification are NOT in v0.1.0.
The `providers/` registry is the clean extension point for v1.1+.
"""
