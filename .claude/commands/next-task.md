Read `TASKS.md` and implement the next not-yet-completed task in order.

Before marking it done: run the chassis's own test suite, add or extend a test citing the task's TS-NNN ID (see `TEST-SCENARIOS.md`), and — if the task has a browser surface — write a Playwright end-to-end test that asserts the rendered result.

If completing the task requires a genuinely minor addition to REQUIREMENTS.md, DESIGN.md, TASKS.md, or TEST-SCENARIOS.md, make the change, note it explicitly in your summary, and flag it for review — do not silently expand scope beyond what the spec describes.
