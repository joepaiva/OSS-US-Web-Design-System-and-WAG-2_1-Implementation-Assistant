---
applyTo: "app/slots/**"
---
# Slots — implement here, spec-first
Implement exactly what `DESIGN.md` specifies for the matching `REQUIREMENTS.md` item — no extra scope. Consume the chassis's provided primitives; do not reimplement them. Keep any control annotations truthful. Add/extend tests for the task's TS-NNN and make them pass without weakening them.
